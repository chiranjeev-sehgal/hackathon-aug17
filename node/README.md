# AI Support Assistant — Node backend

**Requires Node.js 20+** (`node -v`).

```bash
npm install
npm start          # http://localhost:3000
npm test
npm run db:clear   # clears shared ../data and reseeds users
npm run grader
```

See repo-root `README.md` and `TRAINEE_BRIEF.md`.
